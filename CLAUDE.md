# PharmoForge

創薬支援ツール群のプロジェクト。~/work配下のdd_*系プロジェクトとは独立した別系統。

## プロジェクト・サブプロジェクト共通要件

### 利用マシン
- Linux (Ubuntu 24.04) + NVIDIA GeForce GTX 1660 Ti
- Mac (Intel、GPUなし)

GPUが使えるマシンと使えないマシンが混在する。GPUを活用する機能は、GPU非搭載環境でもCPUフォールバックで動作するようにする。

### 環境管理
- mamba (conda) でPython環境を構築する。pipは使わない(mambaで用意できないパッケージのみpip許可)。
- Pythonバージョンは3.14を基本とする。
- 環境名は `pharmoforge`(プロジェクト全体で共通の単一環境)。
- パッケージは可能な限りconda-forgeからインストールする。
- RDKitは中核をなすライブラリの一つであり、`rdkit >= 2026.03.5` でpinする。
- これを満たせないパッケージのインストールが必要な場合は、そのパッケージ専用の別環境を作る(該当数は多くないはず)。

### GitHub運用
- リポジトリ名は `PharmoForge`。
- コミット・プッシュは毎回ユーザーに確認してから行う(dd_*系のような自動push運用はしない)。

### ディレクトリ構成
- 機能ごとにサブディレクトリを作る。
- 再利用可能なライブラリは `src` 以下に機能的パッケージとしてまとめる。
- README([README.md](README.md))の機能一覧(`fetcher`、`docking`等)は大まかな分類であり、`src`以下のパッケージ構成はそれよりも技術的な観点で細かく分割する。保守性を優先し、複数の機能から使われる技術要素・独立してテストや差し替えがしやすい単位は都度専用の小さなパッケージに切り出す。
  - 例: 蛋白識別子マッピング(UniProt/ChEMBL相互変換)は`fetcher`固有ではなく横断的に使う技術要素のため、`src/idmap`として独立させた。

### インストール / CLI
- PharmoForge自体は `pip install -e .` でエディタブルインストールして使う前提(サードパーティ依存パッケージのconda-forge優先方針とは別)。
- スクリプトは単一コマンド `pf` に集約する。機能は第一パラメータで指定する。
  例: `pf fetch activities=JAK1_HUMAN --output data/JAK1_HUMAN.tsv`
- サブコマンドの詳細(引数体系等)は各機能を実装する際に個別に検討する。

### 蛋白識別子のマッピング
- UniProt entry name(例: `JAK1_HUMAN`)、UniProt accession(例: `P23458`)、ChEMBL target id(例: `CHEMBL2835`)は相互にマッピングする。蛋白を指定する引数には、どの形式を与えても等しく機能するようにする。

### ドキュメント
- README、プロンプト記録(*_PROMPT.md等)は機能ごとに作成する。
- `src`以下に汎用的な(複数の機能から使われうる)関数・パッケージを作った場合は、[API.md](API.md)にその内容を記録する。

### 実行時の出力
- ライブラリやスクリプトは常にverboseに実行状況を表示する(進捗が確認できる程度。うるさくなりすぎない範囲に留める)。

### テスト・CI
- 標準的な方法で行う(pytest + GitHub Actions を基本とする)。
- テストはClaudeが自動実行しない。ローカルで実行するコマンドを提示し、ユーザー自身が手元で実行して出力を確認する。
